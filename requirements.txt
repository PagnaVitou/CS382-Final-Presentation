# Core (required to run the RAG system)
streamlit
scikit-learn
numpy
pandas

# Embeddings - real semantic understanding instead of TF-IDF
sentence-transformers

# LLM - AI-powered answer generation
anthropic

# Optional upgrades for scale
# faiss-cpu               # or chromadb, for faster vector search with large corpora
# pypdf                   # if you want to ingest PDF documents
